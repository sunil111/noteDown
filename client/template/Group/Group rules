if userid == owner
	show delete button
else
	if( user is already a member)
		disable join and delete button
	else
		show join and disable delete button


username

google: name, email

meteor:PRIMARY> db.groups.find({ "members" : {$elemMatch: { "name" : "nilesh" } } }, { "members.$" :1, _id: 0}).pretty()

 db.groups.aggregate([{$unwind:'$members'},{$project:{details:'$members' , _id:0}}, {$match:{'details.id':"YkuERv956Qcm5uJd9"}}])

User page

1)Your Notes
Create a note

2)Your Groups
Create Groups

3) View Groups

4)Your Remainders
Create a Remainder

Other groups display
$nin 
 